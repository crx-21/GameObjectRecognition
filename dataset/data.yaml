# dataset/data.yaml
path: .  # root directory of your dataset (relative to data.yaml location)
train: train/images  # directory containing your training images
val: valid/images    # directory containing your validation images

# Classes (must match config.py CLASS_NAMES)
nc: 2
names:
  0: CT
  1: T
